import styled from "styled-components";

const Header = () => {
  return <Title>COUNTRY QUIZ</Title>;
};

const Title = styled.header`
  font-weight: 700;
  font-size: 36px;
  color: #f2f2f2;
`;

export default Header;
